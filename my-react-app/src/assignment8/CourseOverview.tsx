#include<iostream>
using namespace std;



   class Employee { 
    private:
        string name;
        int salary;
    public:
    
    
int main () {
   Student s1;
   s1.setStudents("John", 85);
   s1.setStudents("Doe", 90);
   s1.getmarks();
   s1.display();
    BankAccount account;
    account.deposit(1000);
    account.withdraw(900);
    account.displayBalance();


return 0;
}